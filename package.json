{
  "name": "genlegal-ai",
  "type": "module",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "lint": "genvm-lint check contracts/smart_contract_auditor.py --json",
    "test:direct": "pytest tests/direct/ -v",
    "test:integration": "gltest tests/integration/ -v -s",
    "deploy": "genlayer deploy"
  },
  "devDependencies": {
    "genlayer-js": "^1.1.8"
  }
}
