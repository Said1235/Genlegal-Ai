// .git suffix is for `git clone`, not for browser links - stripped so the
// "View on GitHub" links open the repo's web page correctly.
export const GITHUB_REPO_URL = "https://github.com/Said1235/Genlegal-Ai";

export const APP_VERSION = "0.2.0";

// Official GenLayer explorer - used as fallback when tx-specific URLs
// return 404. Users can paste their transaction hash there manually.
export const GENLAYER_EXPLORER_URL = "https://studio.genlayer.com";
